[package]
name = "ehrelease"
version = "1.0.0"
edition = "2024"
description = "Does stupid work for you so you don't have to"
license = "Apache-2.0"

[[bin]]
name = "ehrelease"
path = "src/main.rs"

[dependencies]
anyhow = "1"
base64 = "0.22"
clap = { version = "4", features = ["derive"] }
jsonwebtoken = "9"
log = "0.4"
env_logger = "0.11"
regex = "1"
reqwest = { version = "0.12", default-features = false, features = ["blocking", "json", "rustls-tls"] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
toml = "0.9"

[profile.release]
strip = true
lto = true